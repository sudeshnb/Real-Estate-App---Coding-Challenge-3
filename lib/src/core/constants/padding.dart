import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppPadding {
  static final h20 = EdgeInsets.symmetric(horizontal: 20.w);
  static final h10 = EdgeInsets.symmetric(horizontal: 10.w);

  static final v5 = EdgeInsets.symmetric(vertical: 5.w);
  static final v15 = EdgeInsets.symmetric(vertical: 15.w);

  static final main = EdgeInsets.all(24.w);

  static final all15 = EdgeInsets.all(15.w);

  static final all5 = EdgeInsets.all(5.w);

  static final all10 = EdgeInsets.all(10.w);

  // static const card = EdgeInsets.all(16.0);
  static final button = EdgeInsets.symmetric(horizontal: 24.w, vertical: 12);

  // static const cardMargin = EdgeInsets.only(top: 24.0);
}
